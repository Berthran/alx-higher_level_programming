-- Create a table called first_table in the current database
-- in the running MySQL server.
-- If the table already exists, do nothing.
CREATE TABLE IF NOT EXISTS first_table (
	id INT,
	name VARCHAR(256)
	);
