-- A script that creates a database named "hn=btn_0c_0 in the
-- running MySQL server.
-- It creates the database if it does not exist, and does nothing
-- if it exists.
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
